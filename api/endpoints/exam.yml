addInviteEmail:
  name: ${opt:stage, 'dev'}-api--add-invite-email
  handler: src/functions/exams.addInviteEmail
  description: add invite email for exam
  tags:
    website: Complect App
    type: API
    subtype: Exam
  layers:
    - {Ref: BaseLibsLambdaLayer}
  events:
    - http:
        path: exam/add-invite-email
        method: post
        cors: true
