recordUpload:
  name: ${opt:stage, 'dev'}-api--recordUpload
  handler: src/functions/record.upload
  description: API endpoint for Record Upload
  memorySize: 2056
  timeout: 60
  tags:
    website: Complect App
    type: API
    subtype: Data
  layers:
    - {Ref: BaseLibsLambdaLayer}
  events:
    - http:
        path: /records/upload
        method: post
        cors: true
